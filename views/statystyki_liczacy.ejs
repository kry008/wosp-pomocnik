<!DOCTYPE html>
<html lang="pl">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %> - Statystyki liczących
    </title>
    <link rel="stylesheet" href="/datatables/dataTables.min.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: black;
            min-height: 100vh;
            padding: 20px
        }

        .header {
            background: white;
            padding: 1rem 2rem;
            border-radius: 10px;
            margin-bottom: 2rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
            flex-wrap: wrap
        }

        .header h1 {
            color: #333
        }

        .header p {
            color: #666
        }

        .content {
            background: white;
            padding: 2rem;
            border-radius: 10px
        }

        .back-btn {
            background: #667eea;
            color: white;
            padding: 0.5rem 1rem;
            border-radius: 5px;
            text-decoration: none;
            display: inline-block
        }

        .back-btn:hover {
            background: #5568d3
        }

        table {
            width: 100%;
            margin-top: 1rem
        }

        .stats-summary {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 1rem;
            margin-bottom: 2rem
        }

        .stat-card {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 1.5rem;
            border-radius: 8px;
            text-align: center
        }

        .stat-card h3 {
            font-size: 2rem;
            margin-bottom: 0.5rem
        }

        .stat-card p {
            opacity: 0.9
        }

        .medal {
            font-size: 1.5rem;
            margin-right: 0.5rem
        }

        .rank-1 {
            background: linear-gradient(135deg, #FFD700 0%, #FFA500 100%);
            font-weight: bold
        }

        .rank-2 {
            background: linear-gradient(135deg, #C0C0C0 0%, #999 100%)
        }

        .rank-3 {
            background: linear-gradient(135deg, #CD7F32 0%, #8B4513 100%)
        }

        /* Mobile improvements */
        .table-responsive { overflow-x: auto; -webkit-overflow-scrolling: touch; }
        @media (max-width: 768px) {
            table { font-size: 0.95rem; }
            table th, table td { padding: 0.5rem; }
            .stat-card h3 { font-size: 1.3rem; }
            .stat-card p { font-size: 0.9rem; }
            .medal { font-size: 1.4rem }
        }
        @media (max-width: 420px) {
            .stats-summary { grid-template-columns: 1fr; gap: 0.75rem; }
            .stat-card { padding: 1rem }
            table { font-size: 0.9rem }
            table th, table td { padding: 0.4rem }
        }
    </style>
</head>

<body>
    <div class="header">
        <div>
            <h1>
                <%= title %>
            </h1>
            <p>Statystyki osób liczących</p>
        </div>
        <div>
            <p style="color: #333;"><strong>Zalogowany:</strong>
                <%= user.imie %>
                    <%= user.nazwisko %>
            </p>
            <a href="/statystyki" class="back-btn">← Wróć</a>
        </div>
    </div>

    <div class="content">
        <h2>Ranking osób liczących</h2>

        <% if (liczacy && liczacy.length> 0) { %>
            <% const topLiczacy = liczacy.slice(0, 3);
               const totalSumaValue = (typeof totalSuma !== 'undefined') ? totalSuma : liczacy.reduce((sum, l)=> sum + (l.suma || 0), 0);
               const totalRozliczenValue = (typeof totalRozliczen !== 'undefined') ? totalRozliczen : liczacy.reduce((sum, l) => sum + (l.liczbaRozliczen || 0), 0);
            %>

                <div class="stats-summary">
                    <div class="stat-card">
                        <h3>
                            <%= liczacy.length %>
                        </h3>
                        <p>Osób liczących</p>
                    </div>
                    <div class="stat-card">
                        <h3>
                            <%= totalRozliczenValue %>
                        </h3>
                        <p>Rozliczeń ogółem</p>
                    </div>
                    <div class="stat-card">
                        <h3>
                            <%= (Number(totalSumaValue) || 0).toFixed(2) %> zł
                        </h3>
                        <p>Suma przeliczona</p>
                    </div>
                </div>

                <div class="table-responsive">
                <table id="liczacyTable" class="display">
                    <thead>
                        <tr>
                            <th>Miejsce</th>
                            <th>Imię i nazwisko</th>
                            <th>Liczba rozliczeń</th>
                            <th>Suma przeliczona</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% liczacy.forEach((l, index)=> { %>
                            <% let rankClass='' ; if (index===0) rankClass='rank-1' ; else if (index===1)
                                rankClass='rank-2' ; else if (index===2) rankClass='rank-3' ; %>
                                <tr class="<%= rankClass %>">
                                    <td>
                                        <% if (index===0) { %>
                                            <span class="medal">🥇</span>
                                            <% } else if (index===1) { %>
                                                <span class="medal">🥈</span>
                                                <% } else if (index===2) { %>
                                                    <span class="medal">🥉</span>
                                                    <% } %>
                                                        <%= index + 1 %>.
                                    </td>
                                    <td>
                                        <%= l.imie %>
                                            <%= l.nazwisko %>
                                    </td>
                                    <td>
                                        <%= l.liczbaRozliczen %>
                                    </td>
                                    <td>
                                        <%= (l.suma || 0).toFixed(2) %> zł
                                    </td>
                                </tr>
                                <% }); %>
                    </tbody>
                </table>
                </div>
                <% } else { %>
                    <p style="text-align: center; padding: 3rem; color: #999;">Brak danych o osobach liczących.</p>
                    <% } %>
    </div>

    <div style="height:20px"></div>

    <script src="/jquery-3.7.1.min.js"></script>
    <script src="/datatables/dataTables.min.js"></script>
    <script>
        $(document).ready(function () {
            $('#liczacyTable').DataTable({
                language: {
                    url: '/datatables/pl.json'
                },
                order: [[3, 'desc']],
                pageLength: 25,
                scrollX: true,
                responsive: true,
                autoWidth: false,
                columnDefs: [
                    { orderable: false, targets: 0 }
                ]
            });
        });
    </script>
</body>

</html>

<!DOCTYPE html>
<html lang="pl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/datatables/dataTables.min.css">
    <link rel="stylesheet" href="/styles.css">
    <style>
        table tr:hover { background:#f8f9fa }
        .no-data { text-align:center; padding:2rem; color:#666; font-style:italic }
    </style>
</head>
<body>
    <div class="header">
        <div style="display: flex; align-items: center;">
            <img src="/logo.png" alt="Logo WOŚP" style="height: 50px; margin-right: 10px;">
            <h1 style="margin: 0;"><a href="/" style="text-decoration: none; color: inherit;"><%= title %></a></h1>
            <p style="margin: 0 0 0 10px;">Ostatnie operacje (audit_log)</p>
        </div>
        <div class="user-info">
            <p><strong>Zalogowany:</strong> <%= user.imie %> <%= user.nazwisko %></p>
            <a href="/dashboard" class="back-btn">← Powrót</a>
        </div>
    </div>

    <div class="content">
        <h3>Logi (<%= logs.length %>)</h3>
        <div class="table-container">
            <% if (logs.length > 0) { %>
            <table id="logsTable">
                <thead>
                    <tr>
                        <th>Czas</th>
                        <th>Tabela</th>
                        <th>Rekord ID</th>
                        <th>Operacja</th>
                        <th>Użytkownik</th>
                        <th>Szczegóły</th>
                    </tr>
                </thead>
                <tbody>
                    <% logs.forEach(function(l) { %>
                    <tr>
                        <td><%= l.czasOperacji %></td>
                        <td><%= l.tabela %></td>
                        <td><%= l.rekordID %></td>
                        <td><%= l.operacja %></td>
                        <td><%= l.uzytkownikLogin || '-' %></td>
                        <td><%= l.szczegoly || '-' %></td>
                    </tr>
                    <% }) %>
                </tbody>
            </table>
            <% } else { %>
            <div class="no-data">Brak logów w bazie danych</div>
            <% } %>
        </div>
    </div>

    <div class="footer">
        <p><%= footer_text %></p>
    </div>

    <script src="/jquery-3.7.1.min.js"></script>
    <script src="/datatables/dataTables.min.js"></script>
    <script>
        document.addEventListener('DOMContentLoaded', function () {
            new DataTable('#logsTable', {
                "language": { "url": "/datatables/pl.json" },
                "pageLength": 25,
                "lengthMenu": [ [10,25,50,-1],[10,25,50,"Wszystkie"] ],
                "order": [[0, "desc"]]
            });
        });
    </script>
</body>
</html>

<!DOCTYPE html>
<html lang="pl">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %> - Statystyki wolontariuszy
    </title>
    <link rel="stylesheet" href="/datatables/dataTables.min.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: black;
            min-height: 100vh;
            padding: 20px
        }

        .header {
            background: white;
            padding: 1rem 2rem;
            border-radius: 10px;
            margin-bottom: 2rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
            flex-wrap: wrap
        }

        .header h1 {
            color: #333
        }

        .header p {
            color: #666
        }

        .content {
            background: white;
            padding: 2rem;
            border-radius: 10px
        }

        .back-btn {
            background: #667eea;
            color: white;
            padding: 0.5rem 1rem;
            border-radius: 5px;
            text-decoration: none;
            display: inline-block
        }

        .back-btn:hover {
            background: #5568d3
        }

        table {
            width: 100%;
            margin-top: 1rem
        }

        .stats-summary {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 1rem;
            margin-bottom: 2rem
        }

        .stat-card {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 1.5rem;
            border-radius: 8px;
            text-align: center
        }

        .stat-card h3 {
            font-size: 2rem;
            margin-bottom: 0.5rem
        }

        .stat-card p {
            opacity: 0.9
        }

        .medal {
            font-size: 1.5rem;
            margin-right: 0.5rem
        }

        .rank-1 {
            background: linear-gradient(135deg, #FFD700 0%, #FFA500 100%);
            font-weight: bold
        }

        .rank-2 {
            background: linear-gradient(135deg, #C0C0C0 0%, #999 100%)
        }

        .rank-3 {
            background: linear-gradient(135deg, #CD7F32 0%, #8B4513 100%)
        }

        .podium {
            display: flex;
            justify-content: center;
            align-items: flex-end;
            gap: 1rem;
            margin-bottom: 2rem;
            padding: 2rem 0
        }

        .podium-item {
            background: white;
            border-radius: 10px;
            padding: 1.5rem;
            text-align: center;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
            min-width: 200px
        }

        .podium-item.first {
            background: linear-gradient(135deg, #FFD700 0%, #FFA500 100%);
            color: white;
            transform: scale(1.1)
        }

        .podium-item.second {
            background: linear-gradient(135deg, #C0C0C0 0%, #999 100%);
            color: white
        }

        .podium-item.third {
            background: linear-gradient(135deg, #CD7F32 0%, #8B4513 100%);
            color: white
        }

        .podium-item .medal {
            font-size: 3rem;
            margin-bottom: 0.5rem
        }

        .podium-item .name {
            font-size: 1.2rem;
            font-weight: bold;
            margin-bottom: 0.5rem
        }

        .podium-item .amount {
            font-size: 1.5rem;
            font-weight: bold
        }

        /* Mobile improvements */
        @media (max-width: 768px) {
            .podium {
                flex-direction: column;
                align-items: center;
                gap: 0.75rem;
                padding: 1rem 0;
            }
            .podium-item {
                min-width: auto;
                width: 100%;
                max-width: 420px;
                padding: 1rem;
                box-shadow: 0 6px 18px rgba(0,0,0,0.12);
            }
            .podium-item.first {
                transform: none;
            }
            .podium-item .medal { font-size: 2rem; }
            .podium-item .name { font-size: 1rem; }
            .podium-item .amount { font-size: 1.25rem; }

            /* Table adjustments */
            .table-responsive { overflow-x: auto; -webkit-overflow-scrolling: touch; }
            table { font-size: 0.9rem; }
            table th, table td { padding: 0.5rem; }
        }

        @media (max-width: 420px) {
            .stat-card h3 { font-size: 1.3rem; }
            .stat-card p { font-size: 0.85rem; }
            .podium-item .medal { font-size: 1.6rem; }
            .podium-item .name { font-size: 0.95rem; }
            .podium-item .amount { font-size: 1.05rem; }
        }
    </style>
</head>

<body>
    <div class="header">
        <div>
            <h1>
                <%= title %>
            </h1>
            <p>Statystyki wolontariuszy</p>
        </div>
        <div>
            <p style="color: #333;"><strong>Zalogowany:</strong>
                <%= user.imie %>
                    <%= user.nazwisko %>
            </p>
            <a href="/statystyki" class="back-btn">← Wróć</a>
        </div>
    </div>

    <div class="content">
        <h2>Ranking wolontariuszy</h2>

        <% if (wolontariusze && wolontariusze.length> 0) { %>
            <% const totalSuma=wolontariusze.reduce((sum, w)=> sum + (w.suma || 0), 0); %>

                <div class="stats-summary">
                    <div class="stat-card">
                        <h3>
                            <%= wolontariusze.length %>
                        </h3>
                        <p>Rozliczonych wolontariuszy</p>
                    </div>
                    <div class="stat-card">
                        <h3>
                            <%= totalSuma.toFixed(2) %> zł
                        </h3>
                        <p>Suma zebrana ogółem</p>
                    </div>
                    <div class="stat-card">
                        <h3>
                            <%= (totalSuma / wolontariusze.length).toFixed(2) %> zł
                        </h3>
                        <p>Średnia na wolontariusza</p>
                    </div>
                </div>

                <% if (wolontariusze.length>= 3) { %>
                    <h3>🏆 Podium</h3>
                    <div class="podium">
                        <!-- 2 miejsce -->
                        <div class="podium-item second">
                            <div class="medal">🥈</div>
                            <div class="name">
                                <%= wolontariusze[1].imie %>
                                    <%= wolontariusze[1].nazwisko %>
                            </div>
                            <div>ID: <%= wolontariusze[1].numerID %>
                            </div>
                            <div class="amount">
                                <%= (wolontariusze[1].suma || 0).toFixed(2) %> zł
                            </div>
                        </div>

                        <!-- 1 miejsce -->
                        <div class="podium-item first">
                            <div class="medal">🥇</div>
                            <div class="name">
                                <%= wolontariusze[0].imie %>
                                    <%= wolontariusze[0].nazwisko %>
                            </div>
                            <div>ID: <%= wolontariusze[0].numerID %>
                            </div>
                            <div class="amount">
                                <%= (wolontariusze[0].suma || 0).toFixed(2) %> zł
                            </div>
                        </div>

                        <!-- 3 miejsce -->
                        <div class="podium-item third">
                            <div class="medal">🥉</div>
                            <div class="name">
                                <%= wolontariusze[2].imie %>
                                    <%= wolontariusze[2].nazwisko %>
                            </div>
                            <div>ID: <%= wolontariusze[2].numerID %>
                            </div>
                            <div class="amount">
                                <%= (wolontariusze[2].suma || 0).toFixed(2) %> zł
                            </div>
                        </div>
                    </div>
                    <% } %>

                        <h3 style="margin-top: 2rem;">Pełna lista</h3>
                        <div class="table-responsive">
                        <table id="wolontariuszeTable" class="display">
                            <thead>
                                <tr>
                                    <th>Miejsce</th>
                                    <th>Numer ID</th>
                                    <th>Imię i nazwisko</th>
                                    <th>Zebrana kwota</th>
                                </tr>
                            </thead>
                            <tbody>
                                <% wolontariusze.forEach((w, index)=> { %>
                                    <% let rankClass='' ; if (index===0) rankClass='rank-1' ; else if (index===1)
                                        rankClass='rank-2' ; else if (index===2) rankClass='rank-3' ; %>
                                        <tr class="<%= rankClass %>">
                                            <td>
                                                <% if (index===0) { %>
                                                    <span class="medal">🥇</span>
                                                    <% } else if (index===1) { %>
                                                        <span class="medal">🥈</span>
                                                        <% } else if (index===2) { %>
                                                            <span class="medal">🥉</span>
                                                            <% } %>
                                                                <%= index + 1 %>.
                                            </td>
                                            <td>
                                                <%= w.numerID %>
                                            </td>
                                            <td>
                                                <%= w.imie %>
                                                    <%= w.nazwisko %>
                                            </td>
                                            <td>
                                                <%= (w.suma || 0).toFixed(2) %> zł
                                            </td>
                                        </tr>
                                        <% }); %>
                            </tbody>
                        </table>
                        </div>
                        <% } else { %>
                            <p style="text-align: center; padding: 3rem; color: #999;">Brak rozliczonych
                                wolontariuszy.</p>
                            <% } %>
    </div>

    <div style="height:20px"></div>

    <script src="/jquery-3.7.1.min.js"></script>
    <script src="/datatables/dataTables.min.js"></script>
    <script>
        $(document).ready(function () {
            $('#wolontariuszeTable').DataTable({
                language: {
                    url: '/datatables/pl.json'
                },
                order: [[3, 'desc']],
                pageLength: 25,
                scrollX: true,
                responsive: true,
                autoWidth: false,
                columnDefs: [
                    { orderable: false, targets: 0 }
                ]
            });
        });
    </script>
</body>

</html>

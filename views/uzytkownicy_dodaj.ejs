<!DOCTYPE html>
<html lang="pl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/styles.css">
    <style>
        .content {
            max-width: 800px;
            margin-left: auto;
            margin-right: auto;
        }
        
        .form-group input,
        .form-group select {
            width: 100%;
        }
        
        .form-group select:focus {
            outline: none;
            border-color: #667eea;
        }
        
        .form-group small {
            display: block;
            margin-top: 0.25rem;
            color: #666;
            font-size: 0.875rem;
        }
        
        .submit-btn {
            background: #667eea;
            color: white;
            padding: 0.75rem 2rem;
            border: none;
            border-radius: 5px;
            font-size: 1rem;
            font-weight: 600;
            cursor: pointer;
            transition: background 0.2s ease;
            width: 100%;
        }
        
        .submit-btn:hover {
            background: #5568d3;
        }
        
        .alert {
            padding: 1rem;
            border-radius: 5px;
            margin-bottom: 1rem;
        }
        
        .alert-error {
            background: #ffebee;
            color: #c62828;
            border: 1px solid #ef5350;
        }
    </style>
</head>
<body>
    <div class="header">
        <div style="display: flex; align-items: center;">
            <img src="/logo.png" alt="Logo WOŚP" style="height: 50px; margin-right: 10px;">
            <h1 style="margin: 0;"><a href="/" style="text-decoration: none; color: inherit;"><%= title %></a></h1>
            <p style="margin: 0 0 0 10px;">Dodawanie użytkownika</p>
        </div>
        <div class="user-info">
            <p><strong>Zalogowany:</strong> <%= user.imie %> <%= user.nazwisko %></p>
            <a href="/uzytkownicy/lista" class="back-btn">← Powrót do listy</a>
        </div>
    </div>
    
    <div class="content">
        <h3>Dodaj nowego użytkownika</h3>
        
        <form method="POST" action="/uzytkownicy/dodaj">
            <div class="form-group">
                <label for="login">Login *</label>
                <input type="text" id="login" name="login" required>
                <small>Unikalny login użytkownika</small>
            </div>
            
            <div class="form-group">
                <label for="haslo">Hasło *</label>
                <input type="password" id="haslo" name="haslo" required autocomplete="new-password">
                <small>Hasło użytkownika (będzie zahashowane)</small>
            </div>
            
            <div class="form-group">
                <label for="imie">Imię *</label>
                <input type="text" id="imie" name="imie" required>
            </div>
            
            <div class="form-group">
                <label for="nazwisko">Nazwisko *</label>
                <input type="text" id="nazwisko" name="nazwisko" required>
            </div>
            
            <div class="form-group">
                <label for="dostep">Poziom dostępu *</label>
                <select id="dostep" name="dostep" required>
                    <% if (user.dostep === 0) { %>
                    <option value="0">Superadmin - pełny dostęp do wszystkich funkcji</option>
                    <% } %>
                    <option value="1" selected>Dostęp pełny - zarządzanie wolontariuszami i rozliczeniami</option>
                    <option value="2">Osoba licząca - tylko dodawanie rozliczeń</option>
                </select>
                <small>
                    <% if (user.dostep === 0) { %>
                        Superadmin: pełne uprawnienia<br>
                    <% } %>
                    Dostęp pełny: zarządzanie wolontariuszami i rozliczeniami<br>
                    Osoba licząca: tylko dodawanie rozliczeń
                </small>
            </div>
            
            <button type="submit" class="submit-btn">Dodaj użytkownika</button>
        </form>
    </div>
    
    <div class="footer">
        <p><%= footer_text %></p>
    </div>
</body>
</html>

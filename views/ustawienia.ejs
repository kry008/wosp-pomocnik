<!DOCTYPE html>
<html lang="pl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: black;
            min-height: 100vh;
            padding: 20px;
        }
        
        .header {
            background: white;
            padding: 1rem 2rem;
            border-radius: 10px;
            margin-bottom: 2rem;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
            display: flex;
            justify-content: space-between;
            align-items: center;
            flex-wrap: wrap;
        }
        
        .header h1 {
            color: #333;
            margin-bottom: 0.5rem;
        }
        
        .user-info {
            text-align: right;
        }
        
        .user-info p {
            color: #666;
            margin-bottom: 0.25rem;
        }
        
        .back-btn {
            background: #667eea;
            color: white;
            padding: 0.5rem 1rem;
            text-decoration: none;
            border-radius: 5px;
            font-weight: 600;
            transition: background 0.2s ease;
            display: inline-block;
            margin-top: 0.5rem;
        }
        
        .back-btn:hover {
            background: #5568d3;
        }
        .access-level {
            display: inline-block;
            padding: 0.25rem 0.75rem;
            border-radius: 20px;
            font-size: 0.8rem;
            font-weight: 600;
            text-transform: uppercase;
        }

        .access-level.superadmin {
            background: #ff4757;
            color: white;
        }

        .dashboard-content {
            background: white;
            padding: 2rem;
            border-radius: 10px;
            box-shadow: 0 5px 15px rgba(255, 255, 255, 0.3);
            margin-bottom: 2rem;
        }

        .dashboard-content h3 {
            color: #333;
            margin-bottom: 1rem;
            border-bottom: 2px solid #667eea;
            padding-bottom: 0.5rem;
        }

        .form-group {
            margin-bottom: 1.5rem;
        }

        .form-group label {
            display: block;
            margin-bottom: 0.5rem;
            font-weight: 600;
            color: #333;
        }

        .form-group input[type="text"],
        .form-group input[type="url"] {
            width: 100%;
            padding: 0.75rem;
            border: 1px solid #ddd;
            border-radius: 5px;
            font-size: 1rem;
        }

        .form-group input:focus {
            outline: none;
            border-color: #667eea;
        }

        .btn {
            background: #667eea;
            color: white;
            padding: 0.75rem 1.5rem;
            border: none;
            border-radius: 5px;
            font-size: 1rem;
            cursor: pointer;
            transition: background 0.2s ease;
        }

        .btn:hover {
            background: #5a67d8;
        }

        .logout-btn {
            background: #ff4757;
            color: white;
            padding: 0.75rem 1.5rem;
            text-decoration: none;
            border-radius: 5px;
            font-weight: 600;
            transition: background 0.2s ease;
            display: inline-block;
        }

        .logout-btn:hover {
            background: #ff3c3c;
        }

        .footer {
            text-align: center;
            color: white;
            margin-top: 2rem;
            font-size: 0.9rem;
        }

        .alert {
            padding: 1rem;
            border-radius: 5px;
            margin-bottom: 1rem;
        }

        .alert.success {
            background: #d4edda;
            color: #155724;
            border: 1px solid #c3e6cb;
        }

        .alert.error {
            background: #f8d7da;
            color: #721c24;
            border: 1px solid #f5c6cb;
        }

        @media (max-width: 768px) {
            .header {
                flex-direction: column;
                text-align: center;
            }

            .user-info {
                text-align: center;
                margin-top: 1rem;
            }
        }
    </style>
</head>
<body>
    <div class="header">
        <div style="display: flex; align-items: center;">
            <img src="/logo.png" alt="Logo WOŚP" style="height: 50px; margin-right: 10px;">
            <h1 style="margin: 0;"><a href="/" style="text-decoration: none; color: inherit;"><%= title %></a></h1>
            <p style="margin: 0 0 0 10px;">Zarządzanie użytkownikami</p>
        </div>
        <div class="user-info">
            <p><strong>Zalogowany:</strong> <%= user.imie %> <%= user.nazwisko %></p>
            <a href="/dashboard" class="back-btn">← Powrót do panelu</a>
        </div>
    </div>

    <div class="dashboard-content">
        <h3>Ustawienia systemu</h3>

        <% if (typeof errors !== 'undefined' && errors.length > 0) { %>
            <div class="alert error">
                <ul>
                    <% errors.forEach(error => { %>
                        <li><%= error %></li>
                    <% }) %>
                </ul>
            </div>
        <% } %>

        <% if (success) { %>
            <div class="alert success">
                Ustawienia zostały zapisane pomyślnie.
            </div>
        <% } %>

        <form method="POST" action="/ustawienia">
            <div class="form-group">
                <label for="discord">Discord Webhook URL</label>
                <input type="url" id="discord" name="discord" value="<%= settings.DISCORD || '' %>" placeholder="https://discord.com/api/webhooks/...">
                <small style="color: #666;">URL webhook dla powiadomień Discord. Pozostaw puste, aby wyłączyć.</small>
            </div>

            <div class="form-group">
                <label for="url_for_app">URL aplikacji mobilnej</label>
                <input type="url" id="url_for_app" name="url_for_app" value="<%= settings.URL_FOR_APP || '' %>" placeholder="https://example.com">
                <small style="color: #666;">URL używany przez aplikację mobilną do łączenia się z systemem.</small>
            </div>

            <button type="submit" class="btn">Zapisz ustawienia</button>
        </form>
    </div>

    <div class="footer">
        <p><%= footer_text %></p>
    </div>
</body>
</html>